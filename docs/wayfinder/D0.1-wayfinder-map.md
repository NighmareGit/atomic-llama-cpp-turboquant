# Wayfinder Map — Phase D Layer Pipeline Investigation

**Date:** 2026-07-10
**Branch:** Path-D-Gpipeline-Assembly-Line
**Goal:** Chart investigation tickets to push back the fog on Path-D assembly-line saturation.

---

## Investigation Questions

### Q1 — Split Topology Analysis (D0.2)
What are the exact compute splits per token for A1/A8/A13 models across the 5-GPU topology? Where are the blocking dependencies?

**Key unknowns:**
- Split assignment per layer for each model class
- Per-split compute time distribution from bench traces
- RPC hop timing characteristics
- Blocking dependencies between splits

### Q2 — GPipe KV-ordering (D0.3)
Can Layer N+1 compute overlap with Layer N's KV write? What ordering constraints exist for MoE expert routing? Does MTP coupling require layer-local h_prev persistence?

**Key unknowns:**
- KV write ordering model for GPipe-style pipeline
- Layer-local h_prev persistence requirements
- MoE expert routing constraints across pipeline stages

### Q3 — Path C Baseline (D0.4)
What is the current dual-GPU utilization on romulus (7900 XTX + 3060 Ti)? What RTT and event patterns exist? Cluster (triton) deferred to later session.

**Key unknowns:**
- Per-device RPC splits on romulus dual-GPU
- Event flow patterns for co-located GPUs
- Server GPU duty cycle vs client-driven RPC

### Q4 — Implementation Seam (D0.5)
Where exactly should the GPipe integration seam live in `llama_context`? What are the touch points in `ggml_backend_sched`?

**Key unknowns:**
- Decode entry point selection logic
- KV state management across pipeline stages
- RPC tensor handoff mechanisms

---

## Investigation Tickets

| Ticket | Type | Goal | Blocking | Status |
|--------|------|------|----------|--------|
| D0.2 | research | Map RPC splits per token across 5-GPU | none | complete |
| D0.3 | grilling | Decide KV ordering model for GPipe | D0.2 | complete |
| D0.4 | prototype | Path C baseline on romulus dual-GPU | none | complete |
| D0.5 | task | Implementation seam design for GPipe | D0.2, D0.3 | complete |

---

## Next Actions

Investigation phase complete. Slice 1 (RPC fix + perf gate) and Slice 2 (Path C + profiler v1) delivered.

1. **Slice 3** — `D5-DEEPER-PIPELINE-AGENT-PLAN.md`: Deeper pipelining (n_stages > 2)
2. See `docs/tickets/path-d-slices.md` for full slice roadmap

---

## References

- `docs/rpc-multi-backend-pipeline-plus/DESIGN-path-d-layer-pipeline.md`
- `docs/rpc-multi-backend-pipeline-plus/TRACKING.md`
- `rpc-patch/patch/HANDOVER-SESSION-2026-07-01-phase-d.md`
- `benches/path-b-plus/` — bench trace data