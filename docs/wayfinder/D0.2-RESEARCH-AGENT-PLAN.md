# D0.2 Research Agent Plan — Split Topology Map

**Ticket:** D0.2  
**Type:** research  
**Goal:** Map RPC compute splits per token across 5-GPU topology  
**Agent persona:** `/research` (background agent for reading legwork)

---

## Mission Statement

Investigate the compute split topology for A1/A8/A13 model classes across the 5-GPU production topology. Produce a detailed map showing:
1. Split assignment per layer
2. Per-split compute time distribution
3. RPC hop timing characteristics
4. Blocking dependencies between splits

---

## Input Materials

The agent must read these files before analysis:

| File | Purpose |
|------|---------|
| `benches/path-b-plus/` | Bench trace data directory |
| `rpc-patch/patch/bench-results/` | Raw bench results with timing data |
| `docs/rpc-multi-backend-pipeline-plus/TRACKING.md` | Current topology decisions and metrics |
| `docs/rpc-multi-backend-pipeline-plus/DESIGN-path-d-layer-pipeline.md` | Design document explaining split terminology |
| `docs/rpc-multi-backend-pipeline-plus/CONTEXT.md` | Domain context for RPC operations |
| `rpc-patch/scripts/pathb-hotpath-summary.sh` | Script for parsing telemetry |
| `rpc-patch/scripts/b6-gate-phase0-assembly-bounds.py` | Tool for assembly bounds analysis |

---

## Execution Steps

### Step 1: Explore Bench Directory Structure
```
LIST_DIR: benches/path-b-plus/
LIST_DIR: rpc-patch/patch/bench-results/
LIST_DIR: rpc-patch/patch/bench-results/cluster-4gpu-primary/
LIST_DIR: rpc-patch/patch/bench-results/config-b-matrix/
LIST_DIR: rpc-patch/patch/bench-results/config-c-matrix/
```

Identify:
- Which bench runs used 5-GPU topology
- Which model classes were tested (A1, A8, A13)
- Trace files with `telemetry/` subdirectories

### Step 2: Analyze Split Timing Data
Read key trace files:
```
READ_FILE: rpc-patch/patch/bench-results/cluster-4gpu-primary/summary.md
READ_FILE: benches/path-b-plus/trace-g-4gpu-primary-trace.meta
READ_FILE: benches/path-b-plus/trace-g-4gpu-primary-trace.result
```

Extract per-split metrics:
- `split_total_ms` per split per backend
- `input_wait_copy_ms` per split
- `graph_compute_async_ms` per split
- `event_sync_slot_ms` per split

### Step 3: Parse RPC Hop Timing
From trace data, identify:
- RPC endpoints used (romulus, remus, triton)
- TCP RTT patterns per hop
- Event record / wait durations
- Blocking points in `send_rpc_cmd`

### Step 4: Map Split Assignment
For each model class (A1, A8, A13):
```
A1 (Qwen3.6-35B-A3B MoE):
- Total layers: 48 (from model architecture)
- Typical TS: 16,25,27,5,27 (equal-safe)
- Splits: 0..4 across 5 GPUs

A8 (llama-70B dense):
- Total layers: 80
- Typical TS: 18,13,31,6,32
- Splits: 0..4 across 5 GPUs

A13 (kimi-72B):
- Total layers: 96
- Typical TS: 19,14,30,6,31
- Splits: 0..4 across 5 GPUs
```

Map layer ranges to split IDs using tensor-split logic.

### Step 5: Identify Blocking Dependencies
From `pathb-sync-site-audit.md` and trace analysis:
- Serial split dispatch (for-loop in `ggml_backend_sched_compute_splits`)
- Sample gate blocking (T+1 unknown until T sampled)
- KV write ordering constraints
- RPC flush / drain points

### Step 6: Create Output Document

Write to: `docs/wayfinder/D0.2-split-topology-map.md`

Structure:

```markdown
# D0.2 Split Topology Map

## Model Classes Analyzed

| Model | Layers | TS | Splits | Target G |
|-------|--------|----|--------|----------|
| A1 (35B-A3B MoE) | 48 | 16,25,27,5,27 | 5 | 48.9 t/s |
| A8 (llama-70B) | 80 | 18,13,31,6,32 | 5 | 15.6 t/s |
| A13 (kimi-72B) | 96 | 19,14,30,6,31 | 5 | 16.6 t/s |

## Split Assignment Per Model

### A1 — Qwen3.6-35B-A3B MoE
| Split | Layers | Backend | Device | Notes |
|-------|--------|---------|--------|-------|
| 0 | embed | CPU | romulus | Token embedding |
| 1 | 0-15 | RPC | remus 5060 | ~27% layers |
| 2 | 16-42 | RPC | romulus 3060 | ~56% layers (straggler) |
| 3 | 43-47 | RPC | triton 3090 | ~10% layers |
| 4 | gather | ROCm | romulus 7900 | KV + logits |

### A8 — llama-70B
| Split | Layers | Backend | Device | Notes |
|-------|--------|---------|--------|-------|
| ... | ... | ... | ... | ... |

### A13 — kimi-72B
| Split | Layers | Backend | Device | Notes |
|-------|--------|---------|--------|-------|
| ... | ... | ... | ... | ... |

## Per-Split Compute Time Distribution

(From bench traces, median ms/token)

| Split | A1 ms | A8 ms | A13 ms | Bottleneck |
|-------|-------|-------|--------|------------|
| 0 | 0.1 | 0.1 | 0.1 | negligible |
| 1 | 5.6 | 8.2 | 7.8 | RPC compute |
| 2 | 5.6 | 8.2 | 7.8 | RPC compute (straggler) |
| 3 | 2.1 | 3.5 | 3.2 | RPC compute |
| 4 | 0.3 | 0.4 | 0.4 | gather |

## RPC Hop Timing Analysis

| Hop | RTT p50 | RTT p95 | RTT p99 | Notes |
|-----|---------|---------|---------|-------|
| romulus -> remus | 0.35 ms | 3.68 ms | 71.55 ms | Tail latency spikes |
| romulus -> triton | 0.42 ms | 4.12 ms | 68.23 ms | Stable |
| remus -> romulus | 0.12 ms | 1.23 ms | 5.67 ms | Return path |

## Blocking Dependencies

| Dependency | Source | Impact | Fix Path |
|------------|--------|--------|----------|
| Serial split dispatch | ggml-backend.cpp | 91% serial | GPipe client sched |
| Sample gate | llama-context.cpp | T+1 blocked | GPipe across tokens |
| KV write ordering | KV cache | Gather waits | Producer-consumer events |
| RPC flush/drain | ggml-rpc.cpp | 50s drain | B+13 fixes |

## Recommendations for GPipe

1. Split 1-2-3 are compute-bound; need overlapping dispatch
2. Split 4 gather must wait for all upstream splits
3. KV write ordering: producer-consumer with event signaling
4. RPC hop latency: acceptable for overlap; tail spikes need mitigation
```

---

## Output Deliverables

| File | Purpose |
|------|---------|
| `docs/wayfinder/D0.2-split-topology-map.md` | Main output document |
| `docs/wayfinder/D0.2-analysis-notes.md` | Raw analysis notes (if verbose) |
| `benches/path-b-plus/D0.2-extracted-metrics.csv` | Extracted metrics (if tabular) |

---

## Success Criteria

- [ ] Split assignment mapped for all 3 model classes
- [ ] Per-split compute times extracted from traces
- [ ] RPC hop timing characterized
- [ ] Blocking dependencies identified with code references
- [ ] Recommendations for GPipe implementation provided

---

## Next Ticket Dependency

**D0.3 (KV-ordering grilling)** blocks on this ticket for:
- Understanding current split boundaries
- Identifying KV write points per split
- Determining overlap feasibility

---

*Plan prepared for `/research` agent invocation — 2026-07-10*