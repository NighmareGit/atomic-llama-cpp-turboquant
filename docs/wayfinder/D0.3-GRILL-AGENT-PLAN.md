# D0.3 Grill Agent Plan — GPipe KV-ordering

**Ticket:** D0.3  
**Type:** grilling  
**Goal:** Decide KV write ordering model for GPipe-style layer pipeline  
**Agent persona:** `/grill-with-docs` (interview-driven design sharpening)

---

## Mission Statement

Through relentless interview, sharpen the design questions around KV write ordering for GPipe-style pipeline:
1. Can Layer N+1 compute overlap with Layer N's KV write?
2. What ordering constraints exist for MoE expert routing?
3. Does MTP coupling require layer-local h_prev persistence?
4. What is the correct KV-ordering model for layer pipeline?

---

## Prereqs

This ticket **blocks on D0.2** for:
- Split boundary understanding
- KV write point identification
- Overlap feasibility assessment

---

## Input Materials

The agent must read these files before grilling:

| File | Purpose |
|------|---------|
| `docs/wayfinder/D0.2-split-topology-map.md` | Split assignment and blocking dependencies |
| `docs/rpc-multi-backend-pipeline-plus/DESIGN-path-d-layer-pipeline.md` | Current design hypotheses |
| `docs/rpc-multi-backend-pipeline-plus/CONTEXT.md` | Domain context |
| `docs/adr/0001-b6-ladder-execution-post-b9-null.md` | Prior decisions on KV ordering |
| `src/llama-kv-cache.cpp` | KV cache implementation |
| `src/llama-kv-cache-iswa.cpp` | Sliding window KV implementation |
| `src/llama-context.cpp` | Context management |
| `common/speculative.cpp` | MTP speculative driver |
| `ggml/src/ggml-backend.cpp` | Scheduler split logic |

---

## Grilling Questions

### Q1 — KV Write Overlap
- When does each split write to KV cache?
- Can split N+1 read KV while split N is still writing?
- What are the current synchronization points in `ggml_backend_sched_compute_splits`?
- How does `pipeline_barrier` interact with KV writes today?

### Q2 — MoE Expert Routing
- How are MoE expert IDs determined per token?
- When is the expert routing decision made?
- Can expert routing overlap with KV write?
- What happens if expert routing changes mid-pipeline?

### Q3 — MTP Coupling Constraints
- How does MTP consume `h_prev` from target?
- What is the timing relationship between MTP draft and target verify?
- Can MTP draft stages overlap with layer pipeline stages?
- Does `h_idx` correction affect pipeline ordering?

### Q4 — GPipe Ordering Model
- What is the producer-consumer relationship between splits?
- How do we signal "KV ready for next stage"?
- What ordering guarantees does the current RPC event model provide?
- Can we use `event_record`/`event_wait` for split-to-split signaling?

---

## Execution Steps

### Step 1: Review D0.2 Output
Read `docs/wayfinder/D0.2-split-topology-map.md` to understand:
- Split boundaries per model
- Current blocking points
- KV write locations

### Step 2: Code Review for KV Timing
Examine:
- `llama_kv_cache::seq_rm` — when is KV trimmed?
- `llama_kv_cache_iswa::init_mtp` — cross-attention setup
- `ggml_backend_sched_pipeline_barrier` — what waits?

### Step 3: Interview Questions
Ask targeted questions about each constraint:
- "What invariant prevents split N+1 from reading KV during split N's write?"
- "Where in the code is this invariant enforced?"
- "Can we relax it with event-based signaling?"

### Step 4: Document Decisions
Write to: `docs/adr/0002-gpipe-kv-ordering.md`

Structure:

```markdown
# ADR-0002: GPipe KV Ordering Model

## Status
Proposed / Accepted / Rejected

## Context
(From DESIGN-path-d s2.1, s2.2, s2.3)

## Decision
(Shaped through grilling)

## Consequences
- Positive: ...
- Negative: ...
- Neutral: ...

## References
- D0.2 split topology analysis
- Code locations reviewed
- Alternative considered
```

---

## Output Deliverables

| File | Purpose |
|------|---------|
| `docs/adr/0002-gpipe-kv-ordering.md` | Architecture decision record |
| `docs/wayfinder/D0.3-grill-notes.md` | Raw interview notes |
| Updates to `docs/agents/domain.md` | Domain language additions |

---

## Success Criteria

- [ ] KV write ordering model decided
- [ ] MoE expert routing constraints documented
- [ ] MTP coupling requirements clarified
- [ ] ADR-0002 written with clear decision
- [ ] Domain language updated in `domain.md`

---

## Next Ticket Dependency

**D0.5 (Implementation Seam Design)** blocks on this ticket for:
- Understanding KV ordering requirements
- Identifying integration points
- Defining the GPipe state machine

---

*Plan prepared for `/grill-with-docs` agent invocation — 2026-07-10*